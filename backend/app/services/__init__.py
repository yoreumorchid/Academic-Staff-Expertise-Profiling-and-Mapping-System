"""Service layer (business logic).

Service modules sit between routers and the database / external APIs so
that complex flows (such as the UC-8 hybrid NLP pipeline) can be tested
in isolation from HTTP concerns.
"""
